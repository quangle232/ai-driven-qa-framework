# API module memory

Module-scoped notes for the API surfaces (rest / grpc / graphql).

## Endpoints / contracts covered
- REST: `/auth/login`, `/users` (see api/rest/contracts/openapi.sample.yaml)
- gRPC: casino GameService (see api/grpc/proto/casino/game.proto)
- GraphQL: Users query / CreateUser mutation (see api/graphql/operations.ts)

## Known issues / flaky
- _none yet_
